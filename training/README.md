# Model Training

This directory contains scripts used to train the machine-learning models
used by the Farmer Advisory Agent.

These scripts are not required for normal application usage when pre-trained
models are already available.
